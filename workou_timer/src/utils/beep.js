import useSound from "use-sound";
import { useUserContext } from "./UserContext";

// export const playBeep = () => {
//   const {user} = useUserContext()
//   const [play] = useSound(user.beepSrc, {volume: 0.5});
//   play()
// }